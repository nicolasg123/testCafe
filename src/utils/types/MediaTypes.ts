/**
 * Media types selectable when creating response
 */
export enum MediaTypes {

    applicationJson = "application/json",
    applicationZip = "application/zip",
    textPlain = "text/plain",

    // Add More when necessary
}
