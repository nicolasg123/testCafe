/**
 * Security Scheme Types
 */
export enum SecuritySchemeTypes {
    OAUTH10 = "OAuth 1.0",
    OAUTH20 = "OAuth 2.0",
    BASIC = "Basic Authentication",
    DIGEST = "Digest Authentication",
    PASSTHROUGH = "Pass Through",
}
